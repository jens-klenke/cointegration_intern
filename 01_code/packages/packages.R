if (!require("pacman")) install.packages("pacman")
pacman::p_load(dplyr,
               readr,
               ggplot2,
               permute,
               stringr,
               here,
               purrr,
               BMS,
               monomvn,
               miscTools,
               tictoc,
               glmnet,
               magick,
               stargazer,
               openintro,
               data.table,
               dplyr,
               tidyr,
               qwraps2,
               papeR,
               kableExtra,
               gridExtra,
               cowplot,
               imputeTS,
               modelr,
               tsDyn,
               parallel,
               doParallel,
               foreach, 
               tibble,
               caret,
               broom,
               pryr,
               gam,
               modelr, 
               microbenchmark,
               RcppEigen,
               formattable)

